# 0004 Release 1.5.2

Status: in_progress
Priority: high
Subsystem: release
Depends on: 0003

## Context
Publish the soft-line and list-continuation improvements from 0003 as the next npm patch release
after merging the implementation into `master`.

## Acceptance Criteria
- Root package version is 1.5.2 in `package.json` and `package-lock.json`.
- Release changes are merged into `master`.
- Git tag `v1.5.2` points to the release commit.
- npm reports `slimdown-js@1.5.2` as the published latest version.

## Implementation Notes
- Release only the root `slimdown-js` package; workspace extension versions are unchanged.
- Run the full tests and build before publishing.

## Agent Notes
- 2026-09-07: Started from completed task 0003 and confirmed npm authentication as the package
  owner. The current public package version is 1.5.1.
